# HTTP async + retry
httpx[http2]>=0.27.0
tenacity>=8.5.0

# Scraping headless (BEC-SP via Playwright; Comprasnet via httpx)
playwright>=1.45.0
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Async DB
asyncpg>=0.29.0

# Celery + broker support (modo distribuído — não necessário para standalone)
celery[redis]>=5.4.0
aio-pika>=9.4.0        # RabbitMQ async

# Config
pydantic-settings>=2.4.0

# Elasticsearch (sync direto — opcional)
elasticsearch[async]>=8.0.0
